//! Query use cases — read-only retrievals across the indexed project.
//!
//! Slice 3.2 migrates the existing operations (peek, grep, search, map,
//! tree, read, stats, files, verify, supported) into this module.
